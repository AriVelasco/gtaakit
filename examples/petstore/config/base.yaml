# Base configuration for the Petstore use case.
# Environment-specific files (e.g., prod.yaml) override these values.

base_url: "https://petstore3.swagger.io/api/v3"
default_headers: {}
default_timeout_seconds: 30.0