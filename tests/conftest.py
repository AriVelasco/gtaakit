"""Shared pytest fixtures and configuration for the gtaakit test suite.

Fixtures defined here are available to every test module without import.
As the suite grows, common building blocks (sample responses, fake
clients, configured validators) are added here to avoid duplication.
"""
