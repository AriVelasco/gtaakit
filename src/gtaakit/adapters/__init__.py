"""Adapters for the gtaakit testing framework.

This subpackage contains concrete implementations of the ports defined
in the ports subpackage. Adapters are organised by category: http for
HttpClient implementations, validators for Validator implementations,
reporters for Reporter implementations.
"""
