"""Ports for the gtaakit testing framework.

A port is an abstract interface that the core depends on, with concrete
implementations (adapters) living in the adapters subpackage. This
follows the ports and adapters architectural style (ADR-001 of the
thesis), keeping the core technology-agnostic.
"""
