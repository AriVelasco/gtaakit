"""Configuration management for the gtaakit testing framework.

The Configuration Manager consolidates configuration from multiple
sources (base file, environment-specific file, environment variables,
CLI parameters) into an immutable view consumed by the rest of the
system, as described in Capítol 7.2.1 of the thesis.
"""
